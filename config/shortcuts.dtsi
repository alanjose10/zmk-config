#define Undo      &kp LG(Z)
#define Cut       &kp LG(X)
#define Copy      &kp LG(C)
#define Paste     &kp LG(V)
#define Find      &kp LG(F)
#define Save      &kp LG(S)
#define SelectAll &kp LG(A)

#define Forward   &kp LG(RBKT)
#define Back      &kp LG(LBKT)
#define Next      &kp LS(LG(RBKT))
#define Previous  &kp LS(LG(LBKT))

#define Linux_Undo      &kp LC(Z)
#define Linux_Cut       &kp LC(X)
#define Linux_Copy      &kp LC(C)
#define Linux_Paste     &kp LC(V)
#define Linux_Find      &kp LC(F)
#define Linux_Save      &kp LC(S)
#define Linux_SelectAll &kp LC(A)

#define Linux_Forward   &kp LA(RIGHT_ARROW)
#define Linux_Back      &kp LA(LEFT_ARROW)
#define Linux_Next      &kp LC(TAB)
#define Linux_Previous  &kp LS(LC(TAB))

#define DesktopLeft   &kp LC(LEFT_ARROW)
#define DesktopRight  &kp LC(RIGHT_ARROW)

#define Linux_DesktopLeft   &kp LC(LA(LEFT_ARROW))
#define Linux_DesktopRight  &kp LC(LA(RIGHT_ARROW))

#define TmuxLeader  &kp LC(A)

// Vi shortcuts
// Go back 
#define Vi_C_o &kp LC(O)
// Go forward
#define Vi_C_i &kp LC(I)
